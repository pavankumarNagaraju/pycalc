# pycalc — CLI Calculator with REPL, Tests, and CI (100% coverage)

## Setup
python -m venv .venv
# activate venv, then:
pip install -r requirements.txt
pytest
python -m pycalc

CI: trigger run

![CI](https://github.com/pavankumarNagaraju/pycalc/actions/workflows/ci.yml/badge.svg)

